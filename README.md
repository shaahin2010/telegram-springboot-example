# telegram-springboot-example

to setup telegram bot, open the application.properties file and fill username and token

to run the bot, connect to the internet, then 
```
mvn clean spring-boot:run
```